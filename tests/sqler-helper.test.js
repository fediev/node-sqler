/* eslint-disable prefer-arrow-callback, func-names */
const helper = require('../lib/sqler-helper');

describe('sqler-helper', function() {
  it('sqler-helper test', function() {
    console.info(helper);
  });
});
